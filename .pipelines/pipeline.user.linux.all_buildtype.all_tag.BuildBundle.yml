workspace:
  download_current_artifacts: true
build:
  commands:
    - !!buildcommand
      name: 'Build auoms'
      command: 'build/cdpx_package.sh'
      artifacts:
        - from: 'target'
          include:
            - '*'
